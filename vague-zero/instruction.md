Use ansible to setup docker. Once docker is installed, setup postgres and nestjs.
Create a docker compose file that on upping the docker compose, postgres and nest should be up and hitting localhost:8080/checkdb should run select 1; and return the output.
